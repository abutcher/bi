#!/usr/bin/env python

from distutils.core import setup

setup(
    name = "idea",
    version = "0.01",
    description = "Iterative Dichotomization on Every Attribute, Data Mining Toolkit",
    package_dir = {'' : 'lib'},
    packages = ['idea'],
    )
